# home-core configuration
#
# Local dev:  cp .env.example .env && pnpm dev
# Docker:     cp .env.example .env && docker compose up -d

# Database (defaults work for local dev)
# IMPORTANT: For production Docker deployments, change the password!
DATABASE_URL=postgres://postgres:postgres@localhost:5432/home_dev
HOME_POSTGRES_PASSWORD=postgres
# HOME_POSTGRES_DB=home_dev
# HOME_POSTGRES_USER=postgres
DOCUMENT_STORAGE_PATH=./documents


# Ports (optional - defaults shown)
# HOME_API_PORT=3001
# HOME_DOC_PROCESSOR_PORT=8000
# HOME_WEB_PORT=5173

# API Keys (optional - can also be set via UI)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=AI...

# Pin to specific version (Docker only)
# VERSION=latest
