{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": [
    "COMMIT_SHA",
    "VITE_COMMIT_SHA",
    "HOME_API_PORT",
    "HOME_WEB_PORT",
    "HOME_DOC_PROCESSOR_PORT",
    "HOME_DOC_PROCESSOR_URL"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".vite/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "test": {
      "dependsOn": ["^test"],
      "outputs": ["coverage/**"]
    }
  }
}
