{
  "name": "@ohs/doc-processor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn src.main:app --reload --host 0.0.0.0 --port ${OHS_DOC_PROCESSOR_PORT:-8000}",
    "build": "echo 'Python service - no build step'",
    "start": "uv run uvicorn src.main:app --host 0.0.0.0 --port ${OHS_DOC_PROCESSOR_PORT:-8000}",
    "lint": "uvx ruff check src/",
    "typecheck": "echo 'Python typecheck runs via uv in CI'"
  }
}
